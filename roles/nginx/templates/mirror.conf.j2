# plain 80 for cert challenge
server {
    listen 80 default_server;
    server_name {{ inventory_hostname }};
    root /srv/mirror;
    location / {
        try_files $uri $uri/ =404;
        autoindex on;
    }
}

